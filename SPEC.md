# saehakgi — 교사용 PC 세팅 이전 도구 · 상세 스펙 (v0.1 draft)

> 새 학기, 기존 PC → 새 PC로 각종 설정을 USB 하나로 옮기고, 옮긴 항목만 원래대로 초기화하는 단일 프로그램.
> 공개 저장소(`saehakgi`), MIT License.

---

## 0. 확정된 핵심 결정

| 항목 | 결정 |
|---|---|
| 기술 스택 | **.NET 8 (C#) 자체포함(self-contained) 단일 exe** — 런타임 설치 불필요 |
| UI | WinForms 또는 WPF GUI, 관리자 권한 상승(`requireAdministrator` manifest) |
| 배포 | USB에서 설치 없이 실행. 공개 GitHub `saehakgi`, MIT |
| 브라우저 범위 | **크롬 + 엣지 모두** (둘 다 Chromium) |
| 초기화 모델 | **매니페스트 기반 — 이 도구가 옮긴/변경한 항목만 추적 후 제거** (전체 스냅샷 아님) |
| 프리셋 | **없음** — 매 실행 시 GUI에서 항목 직접 선택 |
| USB 보안 | 번들 전체 **Argon2id(패스프레이즈) + AES-256-GCM** 암호화 (필수) |
| 대상 환경 | 관리자 권한 확보 가능 · 복원 솔루션/보안USB 정책 없음 · 망분리 제약 없음(일반망+NEIS망 동시) |

---

## 1. 동작 흐름

### (A) 내보내기 — 기존 PC
1. USB에서 `saehakgi.exe` 실행 → 관리자 권한 상승
2. GUI에서 옮길 항목 체크 (아래 §2)
3. **패스프레이즈 입력** (번들 암호화 키 파생용)
4. [내보내기] → 각 항목 수집 → 매니페스트 작성 → 암호화 번들을 USB에 저장
5. 결과 리포트(성공/부분성공/실패 항목별) 표시

### (B) 가져오기 — 새 PC
1. USB에서 `saehakgi.exe` 실행 → 관리자 권한 상승
2. 번들 감지 → **패스프레이즈 입력** → 복호화
3. 적용할 항목 선택(전체/부분) → [가져오기]
4. 적용 매니페스트(`applied-manifest.json`)를 **대상 PC 로컬**에 기록 (초기화용)
5. 결과 리포트 표시 + 재로그인 필요 항목 안내(DBSC 등)

### (C) 초기화 — 새 PC (선택)
1. 로컬 `applied-manifest.json` 로드
2. **이 도구가 만든/바꾼 것만** 역순 제거·복원 (사용자 원래 데이터·교육용 필수 프로그램은 불변)
3. 초기화 리포트 표시

---

## 2. 이전 대상 항목

### 2.1 쉬움 / 안정 (파일·레지스트리 복사)
- **북마크/즐겨찾기** (크롬·엣지 `Bookmarks`)
- **GPKI/NPKI 인증서** — 폴더 복사로 안정적 이전
  - 위치 후보: `%LOCALAPPDATA%\LowiSA`, `%APPDATA%\..\LocalLow\NPKI`, `AppData\LocalLow\GPKI` 등 (실제 경로 탐지 로직 필요)
- **사용자 지정 폴더** — 복수 선택, 하위 전체 재귀 복사
- **Windows 설정**
  - 마우스 속도/포인터 (`HKCU\Control Panel\Mouse`)
  - 디스플레이 **배율·야간 모드**만 (해상도는 모니터 상이 시 깨질 수 있어 **제외 — 결정됨**)
  - 전원 계획, 바탕화면 아이콘 배치, 작업표시줄 고정 항목
  - 기본 앱 연결(기본 브라우저 등), IME/한글 입력 설정
  - 환경변수(HKCU), 설치 폰트, Wi-Fi 프로필(`netsh wlan export`)
  - 프린터 · 네트워크 드라이브 매핑

### 2.2 중간 (목록화 후 재실행)
- **설치 프로그램 목록** — `winget export` 기반 목록 추출 → 새 PC에서 재설치 스크립트
  - winget 미지원 프로그램은 목록+다운로드 링크로 안내(수동)
- **시작프로그램 목록** — `HKCU/HKLM ...\Run`, 시작프로그램 폴더

### 2.3 어려움 / 베스트 에포트 (§3 참조)
- **브라우저 비밀번호** — 확장 API 자동 주입 방식 (§3.1, 결정됨)
- **브라우저 쿠키(로그인 세션)** — 확장+네이티브 메시징 방식

---

## 3. 쿠키 · 비밀번호 서브시스템 (핵심 난제)

### 3.1 방식: 브라우저 확장 + 네이티브 메시징
- raw SQLite 직접 조작 ❌ (ABE·DPAPI 충돌, 백신 오탐)
- 대신 브라우저 엔진의 **sanctioned API** 사용 → 엔진이 스스로 복·재암호화

**원본 PC:**
1. .NET 앱이 관리자 권한으로 확장을 **정책 강제설치**
   (`HKLM\SOFTWARE\Policies\Google\Chrome\ExtensionInstallForcelist` + Edge 대응, USB 내 self-host CRX + update manifest)
2. 확장이 `chrome.cookies.getAll({})` 로 평문 쿠키 수집 (httpOnly·partitionKey 포함)
3. 네이티브 메시징으로 .NET 호스트에 전달 → Argon2id+AES-256-GCM → USB

**대상 PC:**
4. 동일하게 확장 강제설치 → 복호화된 쿠키를 `chrome.cookies.set()` 으로 실제 프로필에 주입

**비밀번호(결정: 확장 API 자동 주입):** 원본에서 저장된 로그인 정보를 확장 경유로 수집 → USB 암호화 → 대상에서 자동 주입.
- 크롬/엣지의 저장 비밀번호는 확장의 표준 API로 직접 열람이 제한되므로, 실제 구현은 다음 중 택1로 상세 설계 필요:
  (i) 확장이 password manager UI/`chrome://password-manager` 흐름을 자동화, 또는
  (ii) 네이티브 호스트가 로그인된 원본에서 DPAPI로 복호화(원본 로컬 한정, ABE 미적용 대상) 후 대상에서 확장 주입.
- CSV 경로는 폴백으로만 유지. (오픈 이슈 §7)

### 3.2 원리상 한계 — README/UI에 명시 (재로그인 필요)
- **DBSC / TPM 바인딩 세션**(구글 계정 등): 하드웨어 서명 불가 → 첫 갱신 시 로그아웃. **우회 불가.**
- **서버 토큰 로테이션·만료**: 내보내기~가져오기 지연 동안 무효화될 수 있음
- **CHIPS(파티션 쿠키)**: partitionKey 포함 처리하되 일부 누락 가능
- **일부 사이트 기기지문 바인딩**: 재로그인 요구

### 3.3 백신 오탐 완화 (코드사이닝 없이 — 결정됨)
서명은 하지 않되, 오탐을 실질적으로 낮추는 조치:
1. **아키텍처 자체가 최대 방어**: 확장 + 네이티브 메시징 = LSASS/브라우저 메모리/DPAPI 직접 접근 없음 → 인포스틸러 시그니처 회피 (가장 큰 요인)
2. **패킹/난독화 금지** (UPX 등은 휴리스틱 유발). 순수 빌드 배포
3. **오픈소스 + 재현가능 빌드 + SHA256 공개** → 스캐너·사용자 검증 가능
4. **배포 전 오탐 신고 사전 제출**: Microsoft Defender "Submit a file for analysis" + VirusTotal 다중엔진 확인
5. **GitHub Releases 경유 배포** → SmartScreen 평판 누적(초기엔 경고 뜸)
6. **의심 행위 회피**: %TEMP% 자기복제 실행/콘솔 숨김/비정상 부모 프로세스 생성 금지
7. (선택 업그레이드) 향후 필요 시 **Azure Trusted Signing ≈ 월 US$10** 로 저렴하게 서명 도입 가능
- 크롬 136+ 는 기본 프로필의 원격 디버깅을 차단하므로 CDP 대신 확장 API 채택.

---

## 4. 데이터 · 보안

- **번들 포맷**: `bundle.saehakgi` (암호화 컨테이너) + `manifest.json`(항목/버전/체크섬)
- **키 파생**: Argon2id (m=64MB, t=3, p=4), 랜덤 salt
- **암호화**: AES-256-GCM, 항목별 IV, AuthTag 무결성 검증
- **패스프레이즈 미입력 시 내보내기 불가** (평문 저장 금지)
- 로그: 민감값 마스킹, USB에는 암호문만

## 5. 초기화 매니페스트
`applied-manifest.json` (대상 PC 로컬 저장):
- 생성한 파일/폴더 경로 목록
- 변경한 레지스트리 키 = {경로, 이전값(백업), 새값}
- 강제설치한 확장 정책 항목
- 주입한 쿠키/비밀번호 도메인 목록(값 제외)
→ 초기화 시 역순 복원. 백업 이전값이 있으면 원복, 신규 생성물이면 삭제.

## 6. 범위 밖 (Non-goals, 최소 1차)
- 전체 디스크 이미지/포맷
- OS 계정·SID 이전
- DBSC 우회 (불가능)
- winget 미지원 프로그램의 완전 자동 설치

## 7. 오픈 이슈 (추가 논의)
- [x] 디스플레이: 배율·야간모드만, 해상도 제외 — 결정
- [x] 비밀번호: 확장 API 자동 주입 — 결정 (세부 구현 (i)/(ii) 택1은 구현 단계에서)
- [x] 코드사이닝: 서명 없이 + §3.3 오탐 완화책 — 결정
- [x] GPKI: 표준 후보 경로 전수 탐지 방식 — 결정 (§8 경로표)
- [ ] 확장 CRX 서명·self-host update manifest 구체화 (구현 단계)
- [ ] 비밀번호 주입 세부 구현 (i) UI 자동화 vs (ii) 네이티브 복호화 최종 택1

## 8. GPKI/NPKI 인증서 표준 후보 경로 (전수 탐지)
프로그램이 아래를 모두 스캔하여 존재하는 인증서(쌍: `signCert.der` + `signPri.key` 형태)를 수집:
- `%USERPROFILE%\AppData\LocalLow\NPKI\**`
- `%USERPROFILE%\AppData\LocalLow\GPKI\**`
- `C:\Program Files\NPKI\**`, `C:\Program Files (x86)\NPKI\**`
- `%LOCALAPPDATA%\LowiSA\**`
- 이동식 디스크 루트 `\NPKI\**`, `\GPKI\**` (사용자가 "복사(이동)→이동식 디스크" 했던 경우)
- 드라이브 루트 `<Drive>:\NPKI\**` 전 드라이브 스캔
발견 시 발급기관/사용자 CN을 파싱해 GUI에 목록 표시 후 선택 이전.

## 9. Windows 로그인 비밀번호 초기화 — **1차 범위 제외 (보류, 결정됨)**
- `net user <user> <pw>` (관리자) 로 기술적으로 가능하나, 강제 변경 시 해당 계정의 **DPAPI 보호 데이터(저장된 브라우저 비밀번호·쿠키·자격증명)가 접근 불가**가 될 수 있어 마이그레이션 목적과 충돌.
- 부작용이 커서 1차 범위에서 제외하고, 이식(migration) 안정화 후 안전한 순서/경고 설계로 재검토.
